eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/

management:
  endpoints:
    enabled.by-default: true
    web:
      exposure:
        include: "*"
      health:
        show-details: always
